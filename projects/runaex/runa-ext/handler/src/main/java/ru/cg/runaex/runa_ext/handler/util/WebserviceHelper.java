package ru.cg.runaex.runa_ext.handler.util;

/**
 * @author urmancheev
 */
public final class WebserviceHelper {

  public static String getUrl(String webserviceName) {
//    UrlSource esbHostSource = DatabaseSpringContext.getEsbHostSource();

//    String url = "http://" + esbHostSource.getHost() + ":" + esbHostSource.getPort() + esbHostSource.getPath();
//    if (!url.endsWith("/"))
//      url = url.concat("/");
//    url = url.concat(webserviceName).concat("?wsdl");
//
//    return url;
    return null;
  }
}
