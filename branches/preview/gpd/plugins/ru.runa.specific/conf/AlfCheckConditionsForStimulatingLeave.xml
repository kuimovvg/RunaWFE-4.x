<config>
  <input>
    <param name="employee" formatFilter="string" label="Сотрудник" />
    <param name="from" formatFilter="date" label="Дата с" />
    <param name="to" formatFilter="date" label="Дата по" />
    <param name="threshold" variable="false" label="Порог опозданий в месяц, часов" optional="true" help="Если опозданий меньше заданного значения, то они не учитываются" />
  </input>
  <output>
    <param name="directorApprovementCause" formatFilter="string" label="Причина рассмотрения директором" help="Выберите переменную, в которую будет записан результат" />
  </output>
</config>
