<?xml version="1.0" encoding="UTF-8"?>
<project name="WFE.common targets">

	<dirname property="WFE.common targets.basedir" file="${ant.file.WFE.common targets}"/>
	<import file="${WFE.common targets.basedir}/../common.build.xml"/>

	<property name="dist.deploy.dir" location="${dist.dir}/deploy" />
	<property name="dist.conf.dir" location="${dist.dir}/conf" />
	<property name="adminkit.dir" value="${dist.dir}/adminkit" />

</project>